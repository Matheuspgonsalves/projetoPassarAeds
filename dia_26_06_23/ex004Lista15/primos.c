#include <stdio.h>
#include <stdlib.h>

void primos(int a, int *b, int *c) {
	for(int i = a-1; i >= 2; i--) {
		if(a % i == 0);
	}
}

int main() {
	int m, p1, p2;
	
	printf("\nDigite um numero: ");
	scanf(" %i", &m);
	
	primos(m, &p1, &p2);
}
